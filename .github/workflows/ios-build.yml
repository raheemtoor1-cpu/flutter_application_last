name: Build iOS App

on:
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Check Flutter version
        run: flutter --version

      - name: Get Flutter dependencies
        run: flutter pub get

      - name: Build iOS
        run: flutter build ios --release --no-codesign

      - name: Create iOS artifact
        run: |
          mkdir -p ios-artifact/Payload
          cp -R build/ios/iphoneos/Runner.app ios-artifact/Payload/
          cd ios-artifact
          zip -r timetable-ios.ipa Payload

      - name: Upload iOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: timetable-ios-build
          path: ios-artifact/timetable-ios.ipa
